import React from "react";
import css from "./Header.css";

const Header = () => {
  return (
    <header>
        <div className="logo">
            MOMATO
        </div>
        <div class="login">
            로그인 / 회원가입
        </div>
    </header>
);
};

export default Header;
